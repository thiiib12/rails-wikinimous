<ul>
  <% @articles.each do |article|%>
    <li>"<%= link_to article.title, article_path(article), class: "links" %>" by <span class="author"><%= article.author %></span></li>
    <br>
  <% end %>
</ul>
